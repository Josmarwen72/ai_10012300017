# Core Flask dependencies
flask>=2.3.0
gunicorn>=21.0.0

# ML dependencies - CPU-only PyTorch to reduce build time
# Install PyTorch CPU first to avoid conflicts
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.0+cpu
torchvision==0.19.0+cpu
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0
transformers>=4.30.0

# Additional dependencies
pandas>=1.5.0
httpx>=0.24.0
python-dotenv>=1.0.0
pypdf>=3.0.0
